export type ProfileDataType = {
    id: string,
    name?: string;
    bio: string;
    email: string;
    avatarUrl: string;
    role: string;
}

export type JustEmailType = {
    email: string;
    id: string;
}